const { price } = {
   name: 'Shirt',
   price: 500,
   quantity: 2,
};
console.log(price);

const { name, pprice, brand } = { name: 'Samsung', pprice: 6500, brand: 'Samsung' };

console.log(pprice);
